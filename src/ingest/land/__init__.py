"""The conservative routing index: sources, compilation, publish.

Separate from `ingest.sources` because it is not a forecast layer — no cycle,
no horizon, no schedule. See `docs/land-index-format.md`.
"""
